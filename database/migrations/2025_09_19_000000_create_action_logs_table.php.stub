<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration {
    public function up(): void {
        Schema::create(config('claim-dispatch.table', 'action_logs'), function (Blueprint $t) {
            $t->id();
            $t->string('type', 120)->index();
            $t->json('payload');
            $t->timestamp('end_at')->index();
            $t->timestamp('claimed_at')->nullable()->index();
            $t->timestamp('processed_at')->nullable()->index();
            $t->unsignedInteger('fail_count')->default(0);
            $t->timestamps();
        });
    }

    public function down(): void {
        Schema::dropIfExists(config('claim-dispatch.table', 'action_logs'));
    }
};
